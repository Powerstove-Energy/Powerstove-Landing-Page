# Base URL of the powerstove-agent-api backend (server-side only — never exposed to the
# browser; all backend calls go through Next.js Server Actions, not client fetch()).
AGENT_API_URL=http://localhost:4000
 
